version: "3"

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
      - ./output:/app/output
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=production
    restart: always
